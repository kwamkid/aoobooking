# Supabase (key format ใหม่ — Publishable + Secret, ไม่ใช่ anon/service_role)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# ใช้ตอน db:link / db:types
SUPABASE_PROJECT_REF=

# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Beam Checkout (SaaS billing) — ว่าง = dev mode (จ่ายผ่านทันทีเพื่อเทสต์ flow)
BEAM_API_KEY=
BEAM_MERCHANT_ID=
BEAM_WEBHOOK_SECRET=

# กัน cron ถูกยิงจากคนนอก
CRON_SECRET=
